 JavaApplication6 - OOP Project


This is a simple Java project created using NetBeans IDE.  
The project demonstrates basic Object-Oriented Programming (OOP) concepts in Java.


 Concepts Used
- Encapsulation
- Classes and Objects
- Inheritance (if used)
- Polymorphism (if used)
- Methods and Constructors

---


`src/` → Contains the main source code
 `build.xml` → Build configuration (NetBeans)
 `manifest.mf` → Application metadata
 `.gitignore` → Files ignored by Git

how to use 
1. Open the project in NetBeans IDE
2. Build the project
3. Run the main class inside `src`


This project was created for learning and practicing Java OOP fundamentals.
